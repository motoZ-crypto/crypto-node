{
  "name": "numen-hardhat-sample",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Hardhat 3 compatibility sample for numen (Frontier EVM, ChainId 320262).",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat --network numen test mocha",
    "deploy": "hardhat --network numen run scripts/deploy.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox-mocha-ethers": "^3.0.5",
    "chai": "^6.0.0",
    "ethers": "^6.14.0",
    "hardhat": "^3.4.5",
    "mocha": "^11.0.0"
  }
}
